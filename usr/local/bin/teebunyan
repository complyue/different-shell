#!/usr/bin/env bash

if [ "$#" == "" ];then
  echo "$0" '<logfile> <args...>'
  exit 1
fi

LOG="$1"
shift
if [ ${LOG: -4} != ".log" ]; then
  LOG="${LOG}.log"
fi

"$@" 2>&1 | tee "${LOG}" | bunyan --color
exit ${PIPESTATUS[0]}
